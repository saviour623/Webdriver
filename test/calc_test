
define convert(a) {
       p10 = trunc(log10(a));
       p2  = trunc(log2(a));

       dp = trunc(p10*ln(10) * p2*ln(2));

       dpexp = exp(dp);
       print(a / ((2**p2) * dpexp);
}

define main() {
       a =  2342344534.;

       convert(a);
}

main();