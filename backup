
//link() unlink error: EEXIST ENOENT  (linkat)
//symlink()
//int mkdir(const char * pathname , mode_t mode )
//int rmdir(const char * pathname );
//int remove(const char * pathname );
//DIR *opendir(const char * dirpath );  error: NULL
//DIR *fdopendir(int fd );
//struct dirent *readdir(DIR * dirp );
//int readdir_r(DIR * dirp , struct dirent * entry , struct dirent ** result ); non-reentrant
//ssize_t readlink(const char * pathname , char * buffer , size_t bufsiz );
//unlink fails if directory with EISDIR or EPERM
//scandir()
//void rewinddir(DIR * dirp );
//closedir()
//int dirfd(DIR * dirp );
/**struct dirent {
ino_t d_ino;
char d_name[];
int dir_type; //non portable

}
/**struct dirent *entryp;
size_t len;
len = offsetof(struct dirent, d_name) + NAME_MAX + 1;
entryp = malloc(len);
if (entryp == NULL)
errExit("malloc");
};*/